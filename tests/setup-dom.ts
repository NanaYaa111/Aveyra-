/**
 * Component-test setup: jest-dom matchers for the jsdom-based tests under
 * tests/component. The node-based unit tests use tests/setup.ts instead.
 */
import '@testing-library/jest-dom/vitest';
