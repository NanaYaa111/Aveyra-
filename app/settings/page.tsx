import { SettingsScreenAdapter } from '@/components/figma-ui/SettingsScreenAdapter';

export const metadata = { title: 'Settings' };

export default function SettingsPage() {
  return <SettingsScreenAdapter />;
}
